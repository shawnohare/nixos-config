- [ ] Attempt to migrate to `vim.pack`. Use the folke blessed
  `vim.pack.loader.enable()` or what it is.
- [ ] The new experimental core UI?
- [ ] neovim-treesitter/nvim-treesitter migration
- [ ] Remove some unused plugins.
- [ ] See if we can get rid of `nvim-cmp` in favore of the 0.12 builtin.
