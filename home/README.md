# Home Manager configuration

Installs programs into user's home and manages some program configurations.
The modules here can be used both in stand-alone home-manager mode or when
home-manager is included as a NixOS or nix-darwin module.
